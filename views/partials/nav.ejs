<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Inventario Castores</a>
    <div class="d-flex align-items-center gap-3">
      <!--btn x rol-->
      <% if (typeof usuario !== 'undefined') { %>
        <% if (usuario.idRol === 1) { %>
          <a class="btn btn-sm btn-outline-primary" href="/inventario">Inventario</a>
          <a class="btn btn-sm btn-outline-primary" href="/historial">Historial</a>
        <% } else if (usuario.idRol === 2) { %>
          <a class="btn btn-sm btn-outline-primary" href="/salidas">Salidas</a>
        <% } %>
      <% } %>

      <span class="navbar-text me-2">
        <%= typeof usuario !== 'undefined' ? usuario.nombre : '' %>
      </span>
      <a class="btn btn-sm btn-outline-danger" href="/logout">Salir</a>
    </div>
  </div>
</nav>