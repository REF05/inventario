const express = require('express');
const router  = express.Router();

/* placeholder – falta después */
router.get('/', (req, res) => res.send('Módulo movimientos – en construcción'));

module.exports = router;